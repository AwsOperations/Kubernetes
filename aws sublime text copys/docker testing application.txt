git clone https://github.com/docker/getting-started-app.git


# syntax=docker/dockerfile:1

FROM node:18-alpine
WORKDIR /app
COPY . .
RUN yarn install --production
CMD ["node", "src/index.js"]
EXPOSE 3000

cd /path/to/getting-started-app

docker build -t getting-started .


docker run -dp 127.0.0.1:3000:3000 getting-started

docker container prune -f
docker rmi -f imagename

docker image prune -a

sudo docker system prune -a

# Build the Docker image
docker build -t my-fluentd-image .

# Run the Docker container
docker run -d --name my-fluentd-container -p 24224:24224 my-fluentd-image
docker rmi -f my-fluentd-image

docker exec -it <container_name_or_id> /bin/sh

tail -f /var/log/fluentd/fluentd.log


# To delete particular directories in particular path

find /var/lib/docker/overlay2 -type d -exec rm -r {} +

sudo mkdir -p /var/run/mongodb/
sudo chown -R mongod:mongod /var/run/mongodb/


